env:
  TARGET_PR_BRANCH: "test"
  PR_BRANCH_PREFIX: "dev-test"

name: PR for test branch
on:
  push:
    branches:
      - dev
jobs:
  release_pull_request:
    runs-on: ubuntu-latest
    name: release_pull_request
    steps:
      - uses: actions/checkout@v3
      - run: |
          git config user.name github-actions
          git remote update
          git fetch --all
          git checkout -b "$PR_BRANCH_PREFIX"${{ github.ref_name }} origin/"$TARGET_PR_BRANCH"
          git cherry-pick -m 1 "${GITHUB_SHA}"
          git push -u origin "$PR_BRANCH_PREFIX"${{ github.ref_name }}
          hub pull-request -b "$TARGET_PR_BRANCH" -h "${PR_BRANCH}" -a "${GITHUB_ACTOR}" -m "\"AUTO: ${PR_TITLE}\""
     
# name: PR for test branch
# on:
#   push:
#     branches:
#       - dev
# jobs:
#   release_pull_request:
#     runs-on: ubuntu-latest
#     name: release_pull_request
#     steps:
#     - name: checkout
#       uses: actions/checkout@v1
#     - name: Create PR to branch
#       uses: izc87/github-action-cherry-pick@master
#       with:
#         pr_branch: 'test'
#       env:
#         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         DRY_RUN: false
